<div class="container"><%= simple_form_for [@cocktail, @dose] do |f| %>

  <div class="form-group">
    <%= f.input :description, placeholder: "Choose the dosage e.g. 2 shots" %>
  </div>

  <div class="form-actions">
    <%= f.submit "add my ingredient", class: "btn btn-default btn-sm" %>
  </div>
<% end %>
</div>

